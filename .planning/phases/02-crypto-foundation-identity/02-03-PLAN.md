---
phase: 02-crypto-foundation-identity
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/components/nostra/QRIdentity.tsx
  - src/components/nostra/QRScanner.tsx
  - src/components/nostra/AddContact.tsx
  - src/tests/nostra/qr-identity.test.ts
  - src/tests/nostra/add-contact.test.ts
autonomous: true
requirements:
  - IDEN-04
  - IDEN-05

must_haves:
  truths:
    - "User can view a QR code containing their npub on the My QR screen"
    - "My QR screen shows display name and NIP-05 alias if set"
    - "User can copy their npub from the My QR screen"
    - "User can share QR as image (download/share API)"
    - "User can scan a QR code via camera to add a contact"
    - "User can upload a gallery image containing a QR code"
    - "User can paste an npub string to add a contact"
    - "After adding a contact, chat opens directly (no confirmation step)"
  artifacts:
    - path: "src/components/nostra/QRIdentity.tsx"
      provides: "My QR screen with npub QR code, display name, copy, share"
      exports: ["default (QRIdentity)"]
    - path: "src/components/nostra/QRScanner.tsx"
      provides: "Camera + gallery QR code scanner"
      exports: ["default (QRScanner)"]
    - path: "src/components/nostra/AddContact.tsx"
      provides: "Add contact dialog with scan/paste options"
      exports: ["default (AddContact)"]
  key_links:
    - from: "src/components/nostra/QRIdentity.tsx"
      to: "src/stores/nostraIdentity.ts"
      via: "useNostraIdentity() for npub, displayName, nip05"
      pattern: "useNostraIdentity"
    - from: "src/components/nostra/QRIdentity.tsx"
      to: "qr-code-styling"
      via: "QR code rendering"
      pattern: "QRCodeStyling"
    - from: "src/components/nostra/AddContact.tsx"
      to: "src/lib/nostra/virtual-peers-db.ts"
      via: "create virtual peer from npub"
      pattern: "mapPubkeyToPeerId|createSyntheticUser"
    - from: "src/components/nostra/AddContact.tsx"
      to: "src/lib/nostra/nostr-identity.ts"
      via: "decodePubkey for npub validation"
      pattern: "decodePubkey"
---

<objective>
Build the QR-based identity sharing and contact exchange system: My QR screen (displays user's npub as QR code), QR scanner (camera + gallery), and Add Contact dialog (scan + paste npub). After adding a contact, the app opens the chat directly.

Purpose: IDEN-04 (share identity via QR) and IDEN-05 (add contacts by scanning QR or pasting npub) enable the social graph for downstream messaging.
Output: Three Solid.js components (QRIdentity, QRScanner, AddContact) plus two test files.
</objective>

<execution_context>
@/home/raider/.claude/get-shit-done/workflows/execute-plan.md
@/home/raider/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-crypto-foundation-identity/02-CONTEXT.md
@.planning/phases/02-crypto-foundation-identity/02-RESEARCH.md
@.planning/phases/02-crypto-foundation-identity/02-01-SUMMARY.md

@src/pages/pageSignQR.ts
@src/lib/nostra/virtual-peers-db.ts
@src/lib/nostra/nostra-bridge.ts
@src/stores/nostraIdentity.ts

<interfaces>
<!-- From Plan 01 outputs -->

From src/lib/nostra/nostr-identity.ts:
```typescript
export function decodePubkey(npubOrHex: string): string; // accepts npub1... or hex, returns hex pubkey
export function npubEncode(pubkeyHex: string): string;
```

From src/stores/nostraIdentity.ts:
```typescript
export default function useNostraIdentity(): {
  npub: Accessor<string|null>;
  displayName: Accessor<string|null>;
  nip05: Accessor<string|null>;
  isLocked: Accessor<boolean>;
  protectionType: Accessor<string>;
};
```

From src/lib/nostra/nostra-bridge.ts:
```typescript
export function mapPubkeyToPeerId(pubkey: string): Promise<PeerId>;
export function createSyntheticUser(pubkey: string, displayName?: string): Promise<void>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build QRIdentity screen and QRScanner component</name>
  <files>src/components/nostra/QRIdentity.tsx, src/components/nostra/QRScanner.tsx, src/tests/nostra/qr-identity.test.ts</files>
  <action>
    1. Create `src/components/nostra/QRIdentity.tsx` (Solid.js component):
       - Import `useNostraIdentity` from `@stores/nostraIdentity`
       - Import QRCodeStyling from `qr-code-styling` (already in deps, see pageSignQR.ts for usage pattern)
       - Props: `{onBack?: () => void}`
       - Render:
         (a) Header bar with back button and title "My QR Code"
         (b) QR code containing the user's npub string, rendered into a div ref via QRCodeStyling
         (c) QR styling: 280x280px, rounded dots, dark foreground on white background (Claude's discretion on exact styling)
         (d) Display name below QR (from identity store), or truncated npub if no display name
         (e) NIP-05 alias with green check icon if set (from identity store)
         (f) "Copy npub" button: copies npub to clipboard via navigator.clipboard.writeText()
         (g) "Share QR" button: calls QRCodeStyling.download() to save as PNG, or uses navigator.share() if available (Web Share API)
       - Use classNames helper for conditional styling
       - Follow project component patterns: props typed inline, no React patterns

    2. Create `src/components/nostra/QRScanner.tsx` (Solid.js component):
       - Props: `{onScan: (data: string) => void; onClose: () => void}`
       - Two modes: camera scan and gallery upload
       - Camera mode:
         (a) Request camera via `navigator.mediaDevices.getUserMedia({video: {facingMode: 'environment'}})`
         (b) Render video element, overlay scan frame UI
         (c) Use jsQR library for decoding (install `pnpm add jsqr` — BarcodeDetector not available in Firefox per research)
         (d) Canvas-based frame capture: requestAnimationFrame loop, draw video frame to canvas, pass imageData to jsQR
         (e) On successful decode, call onScan(data) and stop camera
         (f) Handle permission denied: show message with "Upload Image Instead" fallback button
       - Gallery mode:
         (a) File input accepting image/* (hidden, triggered by button)
         (b) Load image to canvas, pass to jsQR for decoding
         (c) On success, call onScan(data)
         (d) On failure, show "No QR code found" message
       - Always show "Upload from Gallery" button alongside camera (not just as fallback)
       - Clean up: stop camera tracks on unmount (onCleanup)

    3. Write `src/tests/nostra/qr-identity.test.ts`:
       - Test QRIdentity renders npub from identity store
       - Test QRIdentity renders display name when set, truncated npub when not
       - Test QRIdentity renders NIP-05 alias with badge when set
       - Test copy button calls navigator.clipboard.writeText with correct npub
       - Mock QRCodeStyling and useNostraIdentity for unit tests
  </action>
  <verify>
    <automated>cd /home/raider/Repository/nostra && pnpm test src/tests/nostra/qr-identity.test.ts</automated>
  </verify>
  <done>QR screen renders npub as QR code, shows display name and NIP-05 badge, copy and share buttons work, QR scanner handles camera + gallery with jsQR decoding</done>
</task>

<task type="auto">
  <name>Task 2: Build AddContact dialog with scan/paste and direct chat open</name>
  <files>src/components/nostra/AddContact.tsx, src/tests/nostra/add-contact.test.ts</files>
  <action>
    1. Create `src/components/nostra/AddContact.tsx` (Solid.js component):
       - Props: `{onClose: () => void}`
       - Two entry points per user decision: FAB in chat list and sidebar menu item (this component is the dialog itself; wiring to FAB/sidebar is done by the caller)
       - Initial view: two buttons stacked — "Scan QR Code" and "Paste npub"
       - Scan QR path:
         (a) Render QRScanner component
         (b) onScan callback: validate scanned data as npub via decodePubkey() from nostr-identity.ts
         (c) If valid npub, proceed to addContact flow
         (d) If not valid npub, show error "Invalid QR code — expected a Nostr npub"
       - Paste npub path:
         (a) Text input with placeholder "npub1..." or "hex pubkey"
         (b) "Add" button validates input via decodePubkey()
         (c) If valid, proceed to addContact flow
         (d) If invalid, show inline error "Invalid npub format"
       - addContact flow (shared by both paths):
         (a) Call decodePubkey() to get hex pubkey
         (b) Call createSyntheticUser(pubkey) from nostra-bridge.ts to create virtual peer
         (c) Call mapPubkeyToPeerId(pubkey) to get peerId
         (d) Close AddContact dialog
         (e) Navigate to chat with peerId (use existing app navigation — check how other components open chats, likely via rootScope or appImManager)
         (f) Async: fetch contact's kind 0 metadata from relay to get display name and NIP-05 alias (fire and forget — display as truncated npub until metadata arrives)
       - Handle contacts with no kind 0 metadata: display as truncated npub (e.g., "npub1abc...xyz") per Claude's discretion from CONTEXT.md

    2. Write `src/tests/nostra/add-contact.test.ts`:
       - Test that valid npub paste calls createSyntheticUser with correct hex pubkey
       - Test that invalid npub shows error message
       - Test that QR scan with valid npub triggers addContact flow
       - Test that after adding contact, dialog closes
       - Mock virtual-peers-db, nostra-bridge, and nostr-identity
  </action>
  <verify>
    <automated>cd /home/raider/Repository/nostra && pnpm test src/tests/nostra/add-contact.test.ts</automated>
  </verify>
  <done>AddContact validates npub (scan or paste), creates virtual peer, opens chat directly, handles invalid input with error messages, all tests pass</done>
</task>

</tasks>

<verification>
All tests pass: `pnpm test src/tests/nostra/qr-identity.test.ts src/tests/nostra/add-contact.test.ts`

ESLint passes: `pnpm lint`
</verification>

<success_criteria>
- My QR screen displays current user's npub as scannable QR code
- QR screen shows display name, NIP-05 badge, copy button, share button
- QR scanner works with camera AND gallery image upload
- Camera permission denied shows graceful fallback to gallery
- Pasting valid npub creates virtual peer and opens chat
- Pasting invalid npub shows error message
- After add contact (any method), chat opens directly — no confirmation step
</success_criteria>

<output>
After completion, create `.planning/phases/02-crypto-foundation-identity/02-03-SUMMARY.md`
</output>
