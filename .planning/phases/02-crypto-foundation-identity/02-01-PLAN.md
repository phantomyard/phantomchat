---
phase: 02-crypto-foundation-identity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - src/lib/nostra/nostr-identity.ts
  - src/lib/nostra/nostr-crypto.ts
  - src/lib/nostra/key-storage.ts
  - src/stores/nostraIdentity.ts
  - src/tests/nostra/nostr-identity.test.ts
  - src/tests/nostra/key-storage.test.ts
  - src/tests/nostra/nip44-crypto.test.ts
autonomous: true
requirements:
  - IDEN-01
  - IDEN-06
  - MSG-03

must_haves:
  truths:
    - "NIP-06 keypair derivation from 12-word mnemonic produces correct npub/nsec matching NIP-06 test vectors"
    - "NIP-44 encrypt/decrypt roundtrip succeeds for arbitrary plaintext"
    - "NIP-17 gift-wrap primitives (rumor, seal, wrap) produce correctly structured kind 14/13/1059 events"
    - "Keys are AES-GCM encrypted before storage — no plaintext nsec in IndexedDB"
    - "Browser-scoped CryptoKey (default protection) encrypts and decrypts keys across page reloads"
  artifacts:
    - path: "src/lib/nostra/nostr-identity.ts"
      provides: "NIP-06 keypair generation, NIP-19 encoding, mnemonic validation"
      exports: ["generateNostrIdentity", "importFromMnemonic", "validateMnemonic", "npubEncode", "nsecEncode"]
    - path: "src/lib/nostra/nostr-crypto.ts"
      provides: "NIP-44 encryption, NIP-17 gift-wrap primitives, conversation key cache"
      exports: ["nip44Encrypt", "nip44Decrypt", "getConversationKey", "createRumor", "createSeal", "createGiftWrap"]
    - path: "src/lib/nostra/key-storage.ts"
      provides: "AES-GCM encrypted key storage in IndexedDB"
      exports: ["saveEncryptedIdentity", "loadEncryptedIdentity", "decryptKeys", "generateBrowserScopedKey", "deriveKeyFromPin"]
    - path: "src/stores/nostraIdentity.ts"
      provides: "Reactive identity store for UI consumption"
      exports: ["default (useNostraIdentity)"]
  key_links:
    - from: "src/lib/nostra/nostr-identity.ts"
      to: "nostr-tools/nip06"
      via: "privateKeyFromSeedWords import"
      pattern: "privateKeyFromSeedWords"
    - from: "src/lib/nostra/nostr-crypto.ts"
      to: "nostr-tools/nip44"
      via: "v2.encrypt/v2.decrypt imports"
      pattern: "nip44\\.v2\\.(encrypt|decrypt)"
    - from: "src/lib/nostra/key-storage.ts"
      to: "crypto.subtle"
      via: "Web Crypto API for AES-GCM"
      pattern: "crypto\\.subtle\\.(encrypt|decrypt|generateKey|deriveKey)"
    - from: "src/stores/nostraIdentity.ts"
      to: "src/lib/rootScope.ts"
      via: "rootScope event listener"
      pattern: "rootScope\\.addEventListener.*nostra_identity"
---

<objective>
Install nostr-tools and build the cryptographic foundation: NIP-06 keypair generation, NIP-19 encoding, NIP-44 encryption primitives, NIP-17 gift-wrap helpers, AES-GCM encrypted key storage, and the reactive identity store. This plan creates every crypto module that downstream plans (onboarding, migration, contacts, settings) depend on.

Purpose: All identity and encryption operations must exist and be tested before any UI or migration work can begin.
Output: Four new modules (nostr-identity.ts, nostr-crypto.ts, key-storage.ts, nostraIdentity store) plus three test files.
</objective>

<execution_context>
@/home/raider/.claude/get-shit-done/workflows/execute-plan.md
@/home/raider/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-crypto-foundation-identity/02-RESEARCH.md

@src/lib/nostra/identity.ts
@src/lib/nostra/nostr-relay.ts
@src/lib/passcode/keyStore.ts
@src/stores/premium.ts
@src/lib/rootScope.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install nostr-tools and create nostr-identity.ts + nostr-crypto.ts with tests</name>
  <files>package.json, src/lib/nostra/nostr-identity.ts, src/lib/nostra/nostr-crypto.ts, src/tests/nostra/nostr-identity.test.ts, src/tests/nostra/nip44-crypto.test.ts</files>
  <behavior>
    - NIP-06 test vector: mnemonic "leader monkey parrot ring guide accident before fence cannon height naive bean" derives private key 7f7ff03d123792d6ac594bfa67bf6d0c0ab55b6b1fdb6249303fe861f1ccba9a and public key 17162c921dc4d2518f9a101db33695df1afb56ab82f5ff3e5da6eec3ca5cd917
    - generateNostrIdentity() returns {mnemonic, privateKey, publicKey, npub, nsec} with valid 12-word mnemonic
    - importFromMnemonic(mnemonic) returns same structure; invalid mnemonic throws
    - npubEncode(pubkey) returns string starting with "npub1"
    - nsecEncode(privkey) returns string starting with "nsec1"
    - validateMnemonic(valid) returns true; validateMnemonic(invalid) returns false
    - nip44Encrypt then nip44Decrypt roundtrip returns original plaintext
    - getConversationKey returns same key for same sender/recipient pair (deterministic)
    - createRumor produces kind 14 event with content and pubkey
    - createSeal produces kind 13 signed event with encrypted content
    - createGiftWrap produces kind 1059 event with ephemeral key and ['p', recipientPk] tag
  </behavior>
  <action>
    1. Run `pnpm add nostr-tools@2.23.3` to install nostr-tools and its transitive @noble/@scure dependencies.

    2. Create `src/lib/nostra/nostr-identity.ts`:
       - Import `privateKeyFromSeedWords` from `nostr-tools/nip06`
       - Import `generateSecretKey, getPublicKey, finalizeEvent, getEventHash` from `nostr-tools/pure`
       - Import `npubEncode, nsecEncode, decode` from `nostr-tools/nip19`
       - Import `generateMnemonic, validateMnemonic` from `@scure/bip39`
       - Import `wordlist` from `@scure/bip39/wordlists/english`
       - Import `hexToBytes, bytesToHex` from `@noble/hashes/utils`
       - Export `generateNostrIdentity()`: generates 12-word mnemonic via @scure/bip39, derives private key via NIP-06 path, derives public key, returns {mnemonic, privateKey (hex), publicKey (hex), npub, nsec}
       - Export `importFromMnemonic(mnemonic: string)`: validates mnemonic, derives keypair same as above, throws on invalid mnemonic
       - Export `validateMnemonic(mnemonic: string): boolean` wrapping @scure/bip39
       - Export `npubEncode(pubkeyHex: string): string` wrapping nip19
       - Export `nsecEncode(privkeyBytes: Uint8Array): string` wrapping nip19
       - Export `decodePubkey(npubOrHex: string): string` that accepts npub or hex and returns hex pubkey
       - Follow project code style: no spaces after if/for, single quotes, no trailing commas, 2-space indent

    3. Create `src/lib/nostra/nostr-crypto.ts`:
       - Import `* as nip44` from `nostr-tools/nip44`
       - Import `generateSecretKey, getPublicKey, finalizeEvent, getEventHash` from `nostr-tools/pure`
       - Import `bytesToHex` from `@noble/hashes/utils`
       - Maintain an in-memory conversation key cache: `Map<string, Uint8Array>` keyed by recipientPubkeyHex. Export `clearConversationKeyCache()` for logout/lock.
       - Export `getConversationKey(senderPrivHex: string, recipientPubHex: string): Uint8Array` — checks cache first, then calls `nip44.v2.utils.getConversationKey(senderPrivHex, recipientPubHex)`, caches result
       - Export `nip44Encrypt(plaintext: string, conversationKey: Uint8Array): string`
       - Export `nip44Decrypt(ciphertext: string, conversationKey: Uint8Array): string`
       - Export `createRumor(content: string, senderSk: Uint8Array, tags?: string[][]): object` — kind 14, unsigned, with id computed via getEventHash
       - Export `createSeal(rumor: object, senderSk: Uint8Array, recipientPk: string): object` — kind 13, NIP-44 encrypted JSON of rumor, signed with finalizeEvent, randomized created_at (within past 48h)
       - Export `createGiftWrap(seal: object, recipientPk: string): object` — kind 1059, ephemeral key, NIP-44 encrypted JSON of seal, tag ['p', recipientPk], randomized created_at
       - Export `unwrapGiftWrap(wrap: object, recipientSk: Uint8Array): {seal: object, rumor: object}` — reverse of wrap+seal for receiving messages

    4. Write tests first (RED), then implement (GREEN). Use NIP-06 test vectors for identity. For NIP-44, test encrypt/decrypt roundtrip with known keypairs.
  </action>
  <verify>
    <automated>cd /home/raider/Repository/nostra && pnpm test src/tests/nostra/nostr-identity.test.ts src/tests/nostra/nip44-crypto.test.ts</automated>
  </verify>
  <done>NIP-06 test vector passes, generateNostrIdentity produces valid npub/nsec, NIP-44 roundtrip works, gift-wrap produces correct kind 14/13/1059 chain, all tests green</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create key-storage.ts + nostraIdentity store with tests</name>
  <files>src/lib/nostra/key-storage.ts, src/stores/nostraIdentity.ts, src/tests/nostra/key-storage.test.ts</files>
  <behavior>
    - generateBrowserScopedKey() returns a non-exportable CryptoKey
    - deriveKeyFromPin("1234", salt) returns a CryptoKey derived via PBKDF2 with 600000 iterations
    - deriveKeyFromPassphrase("mypassword", salt) returns a CryptoKey via PBKDF2 with 600000 iterations
    - saveEncryptedIdentity encrypts seed+nsec with AES-GCM and stores in IndexedDB
    - loadEncryptedIdentity retrieves the encrypted blob from IndexedDB
    - decryptKeys with correct key returns original seed and nsec
    - decryptKeys with wrong key throws (AES-GCM auth failure)
    - Full roundtrip: generate key -> encrypt -> store -> load -> decrypt -> matches original
    - useNostraIdentity() returns reactive signals for npub, displayName, nip05, isLocked, protectionType
  </behavior>
  <action>
    1. Create `src/lib/nostra/key-storage.ts`:
       - Define `EncryptedIdentityRecord` interface matching the research doc layout: {id: 'current', npub, displayName?, nip05?, protectionType: 'none'|'pin'|'passphrase', salt?: Uint8Array, iv: Uint8Array, encryptedKeys: ArrayBuffer, wrappingKeyId?: string, createdAt: number, migratedFrom?: 'ownid'}
       - Use IndexedDB database name 'Nostra.chat' (same as existing), but add a new object store 'nostr-identity' (version bump the DB). Keep backward compatibility with existing 'identity' store for migration.
       - Export `generateBrowserScopedKey(): Promise<CryptoKey>` — `crypto.subtle.generateKey({name: 'AES-GCM', length: 256}, false, ['encrypt', 'decrypt'])`
       - Export `deriveKeyFromPin(pin: string, salt: Uint8Array): Promise<CryptoKey>` — PBKDF2 with 600,000 iterations, SHA-256, returns AES-GCM key
       - Export `deriveKeyFromPassphrase(passphrase: string, salt: Uint8Array): Promise<CryptoKey>` — same as PIN but different function for clarity
       - Export `encryptKeys(data: {seed: string, nsec: string}, key: CryptoKey): Promise<{iv: Uint8Array, ciphertext: ArrayBuffer}>` — JSON.stringify data, encode, AES-GCM encrypt
       - Export `decryptKeys(iv: Uint8Array, ciphertext: ArrayBuffer, key: CryptoKey): Promise<{seed: string, nsec: string}>` — AES-GCM decrypt, JSON.parse
       - Export `saveEncryptedIdentity(record: EncryptedIdentityRecord): Promise<void>` — stores in IndexedDB 'nostr-identity' store
       - Export `loadEncryptedIdentity(): Promise<EncryptedIdentityRecord | null>` — reads from IndexedDB
       - Export `deleteEncryptedIdentity(): Promise<void>` — removes from IndexedDB
       - Export `saveBrowserKey(key: CryptoKey): Promise<void>` — stores CryptoKey in IndexedDB 'nostr-keys' store (structured clone preserves non-exportable keys)
       - Export `loadBrowserKey(): Promise<CryptoKey | null>` — loads CryptoKey from IndexedDB
       - Handle IndexedDB errors gracefully with try/catch

    2. Create `src/stores/nostraIdentity.ts`:
       - Follow existing store pattern (see src/stores/premium.ts as reference)
       - Use createRoot + createSignal for: npub (string|null), displayName (string|null), nip05 (string|null), isLocked (boolean), protectionType ('none'|'pin'|'passphrase')
       - Listen to rootScope events: 'nostra_identity_loaded', 'nostra_identity_locked', 'nostra_identity_unlocked', 'nostra_identity_updated'
       - Export default function `useNostraIdentity()` returning all signals
       - NOTE: rootScope event type definitions will need to be added to rootScope.ts — add the four event types to the BroadcastEvents interface

    3. Write key-storage tests first (RED), then implement (GREEN). Mock IndexedDB with fake-indexeddb or in-memory stubs since Vitest uses jsdom. Test encrypt/decrypt roundtrip, wrong-key rejection, and PIN derivation produces consistent keys for same input.
  </action>
  <verify>
    <automated>cd /home/raider/Repository/nostra && pnpm test src/tests/nostra/key-storage.test.ts</automated>
  </verify>
  <done>AES-GCM roundtrip passes, wrong key rejected, PBKDF2 derivation deterministic, identity store exports reactive signals, all tests green</done>
</task>

</tasks>

<verification>
All three test files pass: `pnpm test src/tests/nostra/nostr-identity.test.ts src/tests/nostra/nip44-crypto.test.ts src/tests/nostra/key-storage.test.ts`

ESLint passes on new files: `pnpm lint`
</verification>

<success_criteria>
- nostr-tools installed and importable
- NIP-06 test vector matches expected output
- NIP-44 encrypt/decrypt roundtrip succeeds
- NIP-17 gift-wrap creates valid kind 14/13/1059 chain
- AES-GCM encrypted storage roundtrip works
- Wrong-key decryption fails with error (not silent corruption)
- Identity store exports reactive signals
- All 3 test files green
</success_criteria>

<output>
After completion, create `.planning/phases/02-crypto-foundation-identity/02-01-SUMMARY.md`
</output>
