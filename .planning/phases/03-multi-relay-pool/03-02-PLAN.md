---
phase: 03-multi-relay-pool
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/nostra/torShield.tsx
  - src/components/nostra/torBanner.tsx
  - src/components/popups/torStatus.tsx
  - src/components/popups/torFallbackConfirm.tsx
  - src/scss/nostra/_tor-ui.scss
  - src/tests/nostra/tor-fallback-confirm.test.ts
autonomous: true
requirements: [PRIV-02, PRIV-03]

must_haves:
  truths:
    - "Shield icon in topbar shows grey during Tor bootstrap, green when active, orange when direct"
    - "Bootstrap banner shows 'Avvio di Tor...' under topbar and disappears when Tor ready"
    - "Orange persistent banner shows 'Connessione diretta - IP visibile ai relay' in fallback mode with Riprova Tor button"
    - "Green confirmation banner appears when Tor reconnects and fades after 3 seconds"
    - "Tor failure shows confirmation popup before switching to direct — never auto-fallback"
    - "Tap on shield icon shows detailed popup with Tor state, connected relays, per-relay latency"
  artifacts:
    - path: "src/components/nostra/torShield.tsx"
      provides: "Shield icon component with 3 color states"
      min_lines: 30
    - path: "src/components/nostra/torBanner.tsx"
      provides: "Bootstrap/fallback/reconnect banners"
      min_lines: 40
    - path: "src/components/popups/torStatus.tsx"
      provides: "Detailed Tor status popup (state, relays, latency)"
      min_lines: 50
    - path: "src/components/popups/torFallbackConfirm.tsx"
      provides: "Confirmation popup for direct fallback"
      min_lines: 30
    - path: "src/scss/nostra/_tor-ui.scss"
      provides: "Styles for shield icon, banners, and popup"
      min_lines: 40
  key_links:
    - from: "src/components/nostra/torShield.tsx"
      to: "rootScope"
      via: "addEventListener('nostra_tor_state')"
      pattern: "nostra_tor_state"
    - from: "src/components/nostra/torBanner.tsx"
      to: "rootScope"
      via: "addEventListener('nostra_tor_state')"
      pattern: "nostra_tor_state"
    - from: "src/components/popups/torStatus.tsx"
      to: "rootScope"
      via: "addEventListener('nostra_relay_state')"
      pattern: "nostra_relay_state"
    - from: "src/components/popups/torFallbackConfirm.tsx"
      to: "PrivacyTransport"
      via: "confirmDirectFallback() callback"
      pattern: "confirmDirectFallback"
---

<objective>
Create the Tor UX components: shield icon with color states, bootstrap/fallback banners, fallback confirmation popup, and detailed Tor status popup. All components react to rootScope events.

Purpose: Users must always know their privacy state — is Tor active? Is their IP exposed? This is a privacy-first app. The UX makes privacy state unambiguous.
Output: Shield icon, banners, confirmation popup, status popup, SCSS styles.
</objective>

<execution_context>
@/home/raider/.claude/get-shit-done/workflows/execute-plan.md
@/home/raider/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-multi-relay-pool/03-CONTEXT.md
@.planning/phases/03-multi-relay-pool/03-RESEARCH.md

@src/components/nostra/
@src/lib/rootScope.ts

<interfaces>
<!-- rootScope events that Plan 01 creates — these components consume them -->
<!-- If Plan 01 hasn't run yet, these types should be added to rootScope first -->

rootScope BroadcastEvents (added by Plan 01):
```typescript
'nostra_tor_state': {
  state: 'bootstrapping' | 'active' | 'direct' | 'failed';
  error?: string;
};
'nostra_relay_state': {
  url: string;
  connected: boolean;
  latencyMs: number;
  read: boolean;
  write: boolean;
};
'nostra_relay_list_changed': Array<{
  url: string;
  read: boolean;
  write: boolean;
  enabled: boolean;
}>;
```

Note: These events are defined in Plan 01 (same wave). If executing before Plan 01, the executor MUST add these types to rootScope.ts first.

From src/components/nostra/ (existing pattern):
```typescript
// Solid.js TSX components with inline prop types
// Import rootScope from '@lib/rootScope'
// Use classNames from '@helpers/string/classNames'
// CSS modules: import styles from './component.module.scss'
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tor shield icon + banners + SCSS</name>
  <files>
    src/components/nostra/torShield.tsx,
    src/components/nostra/torBanner.tsx,
    src/scss/nostra/_tor-ui.scss
  </files>
  <action>
    1. Create `src/components/nostra/torShield.tsx` — Solid.js component:
       - Listens to `rootScope.addEventListener('nostra_tor_state', ...)` via `onMount`/`onCleanup`
       - Renders a shield SVG icon (simple shield path, not an external icon file)
       - Color states via CSS class: `.tor-shield--bootstrapping` (grey #9e9e9e), `.tor-shield--active` (green #4caf50), `.tor-shield--direct` (orange #ff9800), `.tor-shield--failed` (red #f44336)
       - `onClick`: dispatch a custom event or call a passed `onTap` prop to open the detailed popup
       - Props: `onTap?: () => void`
       - Use `createSignal` for reactive state tracking

    2. Create `src/components/nostra/torBanner.tsx` — Solid.js component:
       - Listens to `rootScope.addEventListener('nostra_tor_state', ...)`
       - Banner states (per user decision, text in Italian):
         a. `bootstrapping`: blue/neutral banner "Avvio di Tor..." — positioned under topbar. Disappears when state changes to 'active'.
         b. `direct`: orange persistent banner "Connessione diretta - IP visibile ai relay" with "Riprova Tor" button and dismiss X. Dismissing hides it but it reappears on next app open (store dismiss in sessionStorage, not localStorage).
         c. `active` (after being in `direct`): green banner "Connesso via Tor" that auto-fades after 3 seconds using setTimeout.
         d. `failed`: no banner (the confirmation popup handles this state)
       - "Riprova Tor" button calls `props.onRetryTor()` callback
       - Props: `onRetryTor?: () => void`

    3. Create `src/scss/nostra/_tor-ui.scss`:
       - `.tor-shield` base class: 24x24px, cursor pointer, transition color 0.2s
       - `.tor-shield--bootstrapping`, `--active`, `--direct`, `--failed` color modifiers
       - `.tor-banner` base: full-width, padding 8px 16px, font-size 14px, display flex, align-items center, justify-content space-between, z-index 3 (above chat but below popups)
       - `.tor-banner--bootstrap`: background #e3f2fd (light blue), color #1565c0
       - `.tor-banner--direct`: background #fff3e0 (light orange), color #e65100
       - `.tor-banner--reconnected`: background #e8f5e9 (light green), color #2e7d32, opacity transition for fade-out
       - `.tor-banner__retry-btn`: styled as text button, underlined, same color as banner text
       - `.tor-banner__dismiss`: close icon button, 20x20px
       - Follow Telegram-like styling: clean, minimal, consistent with existing app design

    IMPORTANT: Use `if(` not `if (`, single quotes, no trailing commas, 2-space indent. Import rootScope from `@lib/rootScope`. Use `classNames` from `@helpers/string/classNames`. Components export default function.
  </action>
  <verify>
    <automated>cd /home/raider/Repository/nostra && rtk pnpm exec tsc --noEmit --project tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <done>
    - TorShield component renders shield icon with reactive color based on nostra_tor_state
    - TorBanner shows correct banner text for each Tor state (Italian text per user decision)
    - SCSS styles cover all states with Telegram-like design
    - TypeScript compiles without errors for these files
  </done>
</task>

<task type="auto">
  <name>Task 2: Tor fallback confirmation popup + status popup + test</name>
  <files>
    src/components/popups/torFallbackConfirm.tsx,
    src/components/popups/torStatus.tsx,
    src/tests/nostra/tor-fallback-confirm.test.ts
  </files>
  <action>
    1. Create `src/components/popups/torFallbackConfirm.tsx` — Solid.js popup component:
       - Shows when Tor state is 'failed' (listened via rootScope)
       - Title: "Tor non disponibile"
       - Body: "Continuare con connessione diretta? Il tuo IP sara' visibile ai relay."
       - Two buttons:
         a. "Riprova" (secondary/outline) — calls `props.onRetry()`, closes popup
         b. "Continua" (primary/warning orange) — calls `props.onConfirmDirect()`, closes popup
       - Props: `onRetry: () => void`, `onConfirmDirect: () => void`, `onClose: () => void`
       - Modal overlay (click outside does NOT dismiss — user must choose)
       - CRITICAL: This popup MUST appear before any direct WebSocket connection is made. No auto-fallback.

    2. Create `src/components/popups/torStatus.tsx` — Solid.js popup component:
       - Opens when user taps the shield icon
       - Content:
         a. Tor state: "Attivo" (green) / "Bootstrap..." (grey) / "Diretto" (orange) / "Errore" (red)
         b. Connected relays list: for each relay show URL, status dot (green/red/yellow), latency in ms, read/write indicators
         c. Relay data comes from listening to `nostra_relay_state` events + initial load from pool
       - Props: `relayStates: Array<{url: string, connected: boolean, latencyMs: number, read: boolean, write: boolean}>`, `torState: string`, `onClose: () => void`
       - Style: slide-up popup matching Telegram's popup style, max-height 70vh with scroll for relay list

    3. Create `src/tests/nostra/tor-fallback-confirm.test.ts`:
       - Test that confirmation popup renders with correct Italian text
       - Test that clicking "Continua" calls onConfirmDirect callback
       - Test that clicking "Riprova" calls onRetry callback
       - Test that clicking overlay does NOT close the popup (modal behavior)
       - Note: Use jsdom environment. Test logic/callbacks, not full render (jsdom limitations per Phase 2 pattern).

    Add styles to `src/scss/nostra/_tor-ui.scss` (append):
    - `.tor-popup-overlay`: fixed fullscreen, bg rgba(0,0,0,0.5), z-index 1000, flex center
    - `.tor-popup`: bg white, border-radius 12px, max-width 360px, padding 24px
    - `.tor-popup__title`: font-size 18px, font-weight 600, margin-bottom 12px
    - `.tor-popup__body`: font-size 14px, color #666, margin-bottom 20px
    - `.tor-popup__actions`: display flex, gap 12px, justify-content flex-end
    - `.tor-status-popup`: max-height 70vh, overflow-y auto
    - `.tor-status-relay`: display flex, align-items center, gap 8px, padding 8px 0, border-bottom 1px solid #eee
    - `.tor-status-dot`: 8px circle, inline-block
    - `.tor-status-dot--green`: bg #4caf50, `.tor-status-dot--red`: bg #f44336, `.tor-status-dot--yellow`: bg #ff9800

    IMPORTANT: Follow project code style. Use path aliases. Popup components should be reusable (not tied to specific DOM mount point).
  </action>
  <verify>
    <automated>cd /home/raider/Repository/nostra && rtk pnpm test src/tests/nostra/tor-fallback-confirm.test.ts -x</automated>
  </verify>
  <done>
    - Fallback confirmation popup shows Italian text per user decision
    - Popup is modal — overlay click does not dismiss
    - "Riprova" and "Continua" buttons invoke correct callbacks
    - Status popup displays Tor state and per-relay info with latency
    - Test passes for confirmation popup behavior
  </done>
</task>

</tasks>

<verification>
```bash
cd /home/raider/Repository/nostra && rtk pnpm test src/tests/nostra/tor-fallback-confirm.test.ts -x && rtk pnpm exec tsc --noEmit --project tsconfig.json 2>&1 | head -20
```
</verification>

<success_criteria>
- Shield icon renders with correct color for each Tor state
- Banners show correct Italian text: "Avvio di Tor...", "Connessione diretta - IP visibile ai relay", "Connesso via Tor"
- Fallback confirmation popup blocks auto-fallback — user must explicitly choose Riprova or Continua
- Status popup shows Tor state + relay list with per-relay latency
- Green reconnect banner fades after 3 seconds
- All files compile, test passes
</success_criteria>

<output>
After completion, create `.planning/phases/03-multi-relay-pool/03-02-SUMMARY.md`
</output>
