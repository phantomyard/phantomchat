---
phase: 01-build-pipeline-distribution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/tests/vendor-stubs.test.ts
  - src/tests/build-output.test.ts
  - src/helpers/compareVersion.ts
  - src/components/chat/bubbles.ts
  - src/components/sidebarLeft/index.ts
  - src/helpers/themeController.ts
  - src/lib/appImManager.ts
  - src/lib/passcode/actions.ts
  - src/pages/nostra-onboarding-integration.ts
autonomous: true
requirements:
  - DIST-01
  - DIST-05

must_haves:
  truths:
    - "`pnpm build` exits 0 with no ESLint errors"
    - "All 6 critical vendor stubs export their expected API shapes (verified by pnpm test vendor-stubs.test.ts passing — each of bezierEasing, convertPunycode, fastBlur, prism, emoji, solid-transition-group asserted as correctly-shaped npm wrapper exports)"
    - "No absolute origin URLs appear in the dist/ build output"
  artifacts:
    - path: src/tests/vendor-stubs.test.ts
      provides: Unit tests verifying each critical vendor stub exports the expected API shape
      exports: [describe, it blocks covering bezierEasing, convertPunycode, fastBlur, prism, emoji, solid-transition-group]
    - path: src/tests/build-output.test.ts
      provides: Unit tests verifying no absolute nostra.chat URLs in dist/index.html
      exports: [test checking dist/ for origin-specific URLs]
  key_links:
    - from: src/tests/vendor-stubs.test.ts
      to: src/vendor/bezierEasing.ts
      via: dynamic import
      pattern: "import.*@vendor"
    - from: src/helpers/compareVersion.ts
      to: ESLint keyword-spacing rule
      via: if( without space
      pattern: "if\\(!v1"
---

<objective>
Create Wave 0 test scaffolds for vendor stubs and build output, then fix all 10 ESLint errors that currently prevent `pnpm build` from exiting 0.

Purpose: The build pipeline is currently broken — `pnpm build` exits with code 1 due to ESLint violations. No further plans can rely on a clean build until this is fixed. Test scaffolds must exist before vendor stub and build output verification can run.

Output: Two test files (vendor-stubs.test.ts, build-output.test.ts) and a passing `pnpm build`.
</objective>

<execution_context>
@/home/raider/.claude/get-shit-done/workflows/execute-plan.md
@/home/raider/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-build-pipeline-distribution/01-CONTEXT.md
@.planning/phases/01-build-pipeline-distribution/01-RESEARCH.md

<interfaces>
<!-- Key types the executor needs. Extracted from codebase. -->

From src/stores/appSettings.ts:
```typescript
// setAppSettings returns a Promise (line 21: return rootScope.managers.appStateManager.setByKey(...))
// The return type is inferred as void by TypeScript but DOES return a Promise
// ESLint await-thenable fires as a false positive — fix with suppression comment, NOT by removing await
const setAppSettings: SetStoreFunction<StateSettings> = (...args: any[]) => { ... };
```

From src/stores/appState.ts:
```typescript
// Same pattern — setAppState returns a Promise (line 13)
// Keep all await usages, add // eslint-disable-next-line @typescript-eslint/await-thenable
const setAppState: SetStoreFunction<State> = (...args: any[]) => { ... };
```

ESLint errors to fix (10 total, from actual pnpm build output):

FIX 1 — keyword-spacing (2 errors, auto-fixable):
  src/helpers/compareVersion.ts:2  → change `if (!v1` to `if(!v1`
  src/helpers/compareVersion.ts:3  → change `if (!v2` to `if(!v2`

FIX 2 — no-trailing-spaces (1 error, auto-fixable):
  src/pages/nostra-onboarding-integration.ts:57 → remove trailing whitespace

FIX 3 — await-thenable (6 errors, false positives — add ESLint suppress comment):
  src/components/chat/bubbles.ts:6225          → await this.chat.setAppState(...)
  src/components/sidebarLeft/index.ts:944      → await setAppSettings(...)
  src/helpers/themeController.ts:320           → await setAppSettings(...)
  src/helpers/themeController.ts:422           → await setAppSettings(...)
  src/lib/appImManager.ts:855                  → await setAppState(...)
  src/lib/passcode/actions.ts:57               → await setAppSettings(...)
  src/lib/passcode/actions.ts:93               → await setAppSettings(...)

(7 await-thenable lines — suppress each with: // eslint-disable-next-line @typescript-eslint/await-thenable)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Wave 0 test scaffolds</name>
  <files>src/tests/vendor-stubs.test.ts, src/tests/build-output.test.ts</files>
  <behavior>
    vendor-stubs.test.ts:
    - Test: bezierEasing default export is a function (callable, returns number)
    - Test: convertPunycode default export has toASCII and toUnicode methods
    - Test: fastBlur default export is a function
    - Test: prism default export has highlight method and languages object
    - Test: emoji exports countryCodeToEmoji (aliased getCountryEmoji), toCodePoints, emojiFromCodePoints, encodeEmoji, getEmojiToneIndex, emojiRegExp
    - Test: solid-transition-group exports Transition and TransitionGroup
    build-output.test.ts:
    - Test: dist/index.html does not contain 'https://nostra.chat' string
    - Test: dist/index.html does not contain 'https://web.telegram.org' string
    - Test: dist/index.html asset src/href attributes start with './' not '/'
    - Skip test with describe.skip if dist/ does not exist (pre-build context)
  </behavior>
  <action>
Create src/tests/vendor-stubs.test.ts that imports each critical vendor stub via dynamic import and asserts its exported shape. Use Vitest `describe`/`it` blocks. Import aliases (@vendor/*) resolve correctly in Vitest via the same vite.config.ts aliases.

Create src/tests/build-output.test.ts that reads dist/index.html using node:fs. If the file does not exist (pre-build), skip all tests in the file with `describe.skipIf(!existsSync('dist/index.html'), ...)`. When the file exists, assert no absolute origin URLs appear.

Follow the existing test style from src/tests/cards.test.ts (globals: true, no explicit imports of describe/it).

Do NOT use spaces inside `{}` or `[]`. Use `if(` not `if (`. No trailing commas.
  </action>
  <verify>
    <automated>cd /home/raider/Repository/nostra && pnpm test src/tests/vendor-stubs.test.ts src/tests/build-output.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>Both test files exist and tests pass (build-output tests skipped if dist/ absent — that is a PASS, not a failure).</done>
</task>

<task type="auto">
  <name>Task 2: Fix all 10 ESLint errors</name>
  <files>src/helpers/compareVersion.ts, src/pages/nostra-onboarding-integration.ts, src/components/chat/bubbles.ts, src/components/sidebarLeft/index.ts, src/helpers/themeController.ts, src/lib/appImManager.ts, src/lib/passcode/actions.ts</files>
  <action>
Fix each ESLint violation in-place:

1. src/helpers/compareVersion.ts lines 2-3: Change `if (!v1` → `if(!v1` and `if (!v2` → `if(!v2` (keyword-spacing rule: no space after `if`).

2. src/pages/nostra-onboarding-integration.ts: Run `eslint --fix src/pages/nostra-onboarding-integration.ts` to remove trailing spaces. Verify line 57 has no trailing whitespace.

3. For each of the 7 await-thenable violations, add `// eslint-disable-next-line @typescript-eslint/await-thenable` on the line BEFORE the `await` call. Do NOT remove the `await` — setAppState and setAppSettings genuinely return Promises; TypeScript's inferred type is wrong (SetStoreFunction typed as void but returns Promise). The suppress comment is the correct fix.

After all fixes: run `pnpm lint` (or the ESLint check command from vite.config.ts) to confirm 0 errors before proceeding.

Do NOT add `eslint-disable` file-level comments — only single-line `eslint-disable-next-line` suppressions.
  </action>
  <verify>
    <automated>cd /home/raider/Repository/nostra && pnpm build 2>&1 | tail -30</automated>
  </verify>
  <done>`pnpm build` exits 0 with no ESLint errors and no TypeScript errors (TypeScript checker is still disabled at this point — that is expected).</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `pnpm build` exits 0 — confirmed by Task 2 verify
2. `pnpm test src/tests/vendor-stubs.test.ts src/tests/build-output.test.ts` passes — confirmed by Task 1 verify
3. `grep -r 'https://nostra.chat' dist/ || echo PASS` outputs PASS
4. `grep 'src=\|href=' dist/index.html | head -5` shows `./` prefixed paths (not `/`)
</verification>

<success_criteria>
- `pnpm build` exits with code 0
- Zero ESLint errors in build output
- vendor-stubs.test.ts exists with 6+ test cases covering all critical stubs (pnpm test vendor-stubs.test.ts passes green)
- build-output.test.ts exists with relative-path assertions
- No `eslint-disable-file` or blanket suppressions added (only `eslint-disable-next-line` for the 7 await-thenable false positives)
</success_criteria>

<output>
After completion, create `.planning/phases/01-build-pipeline-distribution/01-01-SUMMARY.md`
</output>
