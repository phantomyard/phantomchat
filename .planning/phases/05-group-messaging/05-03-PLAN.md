---
phase: 05-group-messaging
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - src/components/nostra/GroupCreation.tsx
  - src/components/nostra/GroupCreation.module.scss
  - src/components/nostra/GroupInfo.tsx
  - src/components/nostra/GroupInfo.module.scss
  - src/components/nostra/GroupMemberList.tsx
  - src/components/nostra/GroupMemberList.module.scss
  - src/components/nostra/GroupAvatarInitials.tsx
  - src/components/nostra/GroupAvatarInitials.module.scss
  - src/components/nostra/GroupPrivacySetting.tsx
  - src/components/nostra/GroupPrivacySetting.module.scss
  - src/lib/nostra/nostra-display-bridge.ts
  - src/lib/nostra/group-api.ts
  - src/tests/nostra/group-display.test.ts
autonomous: false
requirements: [GRP-01, GRP-03, GRP-04]

must_haves:
  truths:
    - "User can create a group via multi-step flow: select contacts -> enter name -> create"
    - "Group appears in chat list with avatar initials, name, and preview text"
    - "User can view group info sidebar with avatar, name, member list, and leave button"
    - "Admin can add members from group info sidebar"
    - "Admin can remove members from group info sidebar via context action"
    - "User can leave group from group info sidebar"
    - "Privacy setting allows choosing who can add user to groups"
    - "Group avatar shows CSS initials when no custom avatar set"
  artifacts:
    - path: "src/components/nostra/GroupCreation.tsx"
      provides: "Multi-step group creation flow"
      min_lines: 80
    - path: "src/components/nostra/GroupInfo.tsx"
      provides: "Group info sidebar panel"
      min_lines: 60
    - path: "src/components/nostra/GroupMemberList.tsx"
      provides: "Scrollable member list with admin actions"
      min_lines: 40
    - path: "src/components/nostra/GroupAvatarInitials.tsx"
      provides: "CSS-based initials avatar component"
      min_lines: 15
    - path: "src/components/nostra/GroupPrivacySetting.tsx"
      provides: "Who can add me to groups radio group"
      min_lines: 30
    - path: "src/tests/nostra/group-display.test.ts"
      provides: "Unit tests for group dialog injection and avatar initials"
  key_links:
    - from: "src/components/nostra/GroupCreation.tsx"
      to: "src/lib/nostra/group-api.ts"
      via: "getGroupAPI().createGroup() on form submit"
      pattern: "createGroup"
    - from: "src/components/nostra/GroupInfo.tsx"
      to: "src/lib/nostra/group-store.ts"
      via: "getGroupStore().get(groupId) for metadata"
      pattern: "getGroupStore"
    - from: "src/components/nostra/GroupMemberList.tsx"
      to: "src/lib/nostra/group-api.ts"
      via: "addMember/removeMember calls"
      pattern: "addMember|removeMember"
    - from: "src/lib/nostra/nostra-display-bridge.ts"
      to: "src/lib/nostra/group-store.ts"
      via: "getAll() on app startup to register all group dialogs"
      pattern: "getAll"
---

<objective>
Group UI components: creation flow, info sidebar, member management, initials avatar, and privacy settings.

Purpose: Provides the visual interface for all group operations per the UI-SPEC contract (IC-01 through IC-07). Connects to GroupAPI (Plan 02) for all actions.
Output: 5 Solid.js components with SCSS modules, wired to display bridge and chat topbar, plus group-display tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-group-messaging/05-CONTEXT.md
@.planning/phases/05-group-messaging/05-UI-SPEC.md
@.planning/phases/05-group-messaging/05-02-SUMMARY.md

<interfaces>
<!-- Key types from Plan 01 and Plan 02 that UI consumes -->

From src/lib/nostra/group-types.ts:
```typescript
export interface GroupRecord {
  groupId: string;
  name: string;
  description?: string;
  avatar?: string;
  adminPubkey: string;
  members: string[];
  peerId: number;
  createdAt: number;
  updatedAt: number;
}
```

From src/lib/nostra/group-store.ts:
```typescript
export class GroupStore {
  get(groupId: string): Promise<GroupRecord | undefined>;
  getByPeerId(peerId: number): Promise<GroupRecord | undefined>;
  getAll(): Promise<GroupRecord[]>;
}
export function getGroupStore(): GroupStore;
```

From src/lib/nostra/group-api.ts:
```typescript
export class GroupAPI {
  createGroup(name: string, memberPubkeys: string[], description?: string): Promise<string>;
  sendMessage(groupId: string, content: string, type?: string): Promise<string>;
  addMember(groupId: string, newMemberPubkey: string): Promise<void>;
  removeMember(groupId: string, memberPubkey: string): Promise<void>;
  leaveGroup(groupId: string): Promise<void>;
}
export function getGroupAPI(): GroupAPI;
```

From src/lib/nostra/nostra-display-bridge.ts (extended in Plan 02):
```typescript
export class NostraDisplayBridge {
  createGroupDialog(group: GroupRecord): void;
  injectGroupChat(group: GroupRecord): void;
  injectServiceMessage(groupPeerId: number, text: string): void;
}
```

From src/lib/nostra/virtual-peers-db.ts:
```typescript
export function getAllMappings(): Promise<VirtualPeerMapping[]>;
export function getPubkey(peerId: number): Promise<string | null>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GroupCreation flow + GroupAvatarInitials + GroupPrivacySetting + display tests</name>
  <files>src/components/nostra/GroupCreation.tsx, src/components/nostra/GroupCreation.module.scss, src/components/nostra/GroupAvatarInitials.tsx, src/components/nostra/GroupAvatarInitials.module.scss, src/components/nostra/GroupPrivacySetting.tsx, src/components/nostra/GroupPrivacySetting.module.scss, src/tests/nostra/group-display.test.ts</files>
  <read_first>
    - src/components/nostra/GroupCreation.tsx (will be created, but check if dir exists)
    - src/components/sidebarLeft/tabs/contacts.ts (contact selector pattern to reuse)
    - src/components/popups/ (popup patterns for confirmation dialogs)
    - src/components/peerProfile.tsx (sidebar layout pattern reference)
    - src/helpers/string/classNames.ts (classNames helper usage)
    - src/lib/nostra/group-api.ts (Plan 02 output - GroupAPI)
    - src/lib/nostra/virtual-peers-db.ts (getAllMappings for contact list)
    - src/lib/nostra/nostra-display-bridge.ts (injectGroupChat to test)
    - .planning/phases/05-group-messaging/05-UI-SPEC.md (IC-01, IC-07 interaction contracts)
  </read_first>
  <behavior>
    - Test 1: injectGroupChat() produces a dialog with peer._ === 'peerChat' and negative peerId
    - Test 2: injectGroupChat() uses Math.abs(peerId) for chat_id (always positive)
    - Test 3: GroupAvatarInitials renders the correct initial letter (first char of name, uppercase)
    - Test 4: GroupAvatarInitials produces deterministic color from groupId (same groupId always gives same color from 7-color palette)
    - Test 5: GroupAvatarInitials with different groupIds can produce different colors
    - Test 6: groupIdToPeerId returns a negative number for any valid hex groupId
  </behavior>
  <action>
    **Step 0 — group-display.test.ts:** Write tests FIRST covering all 6 behaviors listed above. Mock IndexedDB and rootScope managers for injectGroupChat tests. For GroupAvatarInitials, test the color derivation function and initial extraction logic (these can be tested as pure functions extracted from the component). Use the same test setup as existing nostra tests in src/tests/nostra/.

    **Step 1 — GroupAvatarInitials.tsx:** Simple CSS-based component per UI-SPEC:

    Props: `{name: string, size?: number, groupId?: string}`. Default size 48px.
    - Extract first letter of name (uppercase)
    - Background color: deterministic from groupId hash using tweb's 7-color avatar palette (same colors as UI-SPEC: #FF845E, #FEBB5B, #B694F9, #9AD164, #53edd6, #5CAFFA, #FF8AAC)
    - Export the pure helper functions `getInitialLetter(name: string): string` and `getAvatarColor(groupId: string): string` for testability
    - Render: `<div class={styles.avatar} style={{'width': size+'px', 'height': size+'px', 'background-color': color}}><span class={styles.initial}>{letter}</span></div>`
    - SCSS: `.avatar` = circle (border-radius 50%), flex centered. `.initial` = white text, font-weight 500, font-size = size * 0.4.

    **Step 2 — GroupCreation.tsx:** Multi-step creation flow per IC-01:

    State: `step: 'contacts' | 'details'`, `selectedPubkeys: string[]`, `groupName: string`, `groupDescription: string`

    **Step 1 (Contact Picker):**
    - Load contacts from `getAllMappings()` (virtual-peers-db)
    - Render scrollable list with checkboxes next to each contact name
    - Selected contacts as chips at top
    - Counter: `{selectedPubkeys.length}/12 members`
    - When length === 12, remaining contacts get opacity 0.3, non-interactive
    - "Continue to Details" button (accent color `var(--primary-color)`, enabled when >= 1 selected)
    - Back/cancel returns to chat list

    **Step 2 (Group Details):**
    - GroupAvatarInitials placeholder (120px, tap shows future photo picker — for now just the initials)
    - Text input: group name (required, max 128 chars, placeholder "Group name")
    - Textarea: description (optional, placeholder "Description (optional)")
    - "Create Group" button (accent color, enabled when name non-empty)
    - On submit: `const groupId = await getGroupAPI().createGroup(groupName, selectedPubkeys, groupDescription)`
    - Then navigate to new group chat via `rootScope.managers.appImManager.setPeer({peerId: groupPeerId})`

    **Step 3 — GroupPrivacySetting.tsx:** Per IC-07:

    Radio group with 3 options stored in localStorage key `'nostra_group_privacy'`:
    - `'everyone'` (default): "Everyone" — Anyone can add you to groups
    - `'contacts'`: "My Contacts" — Only contacts can add you to groups
    - `'nobody'`: "Nobody" — Group invitations go to Message Requests

    Use existing privacy settings pattern from ReadReceiptToggle. Standard tweb radio list styling.

    **Step 4 — Wire GroupCreation trigger:**

    In nostra-display-bridge.ts (or an init file), add a method `mountGroupCreation()` that creates a mount point and renders GroupCreation.tsx when user taps "New Group" button. The trigger button should be added near the existing new-chat FAB or sidebar menu — use rootScope event `'nostra_create_group'` dispatched from wherever the button is placed. GroupCreation listens for this event to show.

    **Step 5 — Wire GroupPrivacySetting:**

    Expose GroupPrivacySetting in the Privacy & Security settings section. Mount similarly to ReadReceiptToggle. The setting value is read by GroupAPI.handleControlMessage when processing group_add_member — if privacy is 'contacts' and sender not in contacts, or privacy is 'nobody', route to message requests instead of auto-join.
  </action>
  <verify>
    <automated>pnpm test src/tests/nostra/group-display.test.ts && pnpm build 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/tests/nostra/group-display.test.ts exists and passes
    - src/components/nostra/GroupCreation.tsx exists and contains `createGroup`
    - src/components/nostra/GroupCreation.tsx contains `selectedPubkeys` state
    - src/components/nostra/GroupCreation.tsx contains `12` (member limit check)
    - src/components/nostra/GroupCreation.tsx contains `Continue to Details` (copy from UI-SPEC)
    - src/components/nostra/GroupCreation.tsx contains `Create Group` (copy from UI-SPEC)
    - src/components/nostra/GroupAvatarInitials.tsx exists and exports `getInitialLetter` and `getAvatarColor`
    - src/components/nostra/GroupAvatarInitials.module.scss contains `border-radius: 50%`
    - src/components/nostra/GroupPrivacySetting.tsx exists and contains `nostra_group_privacy`
    - src/components/nostra/GroupPrivacySetting.tsx contains `everyone` and `contacts` and `nobody`
    - pnpm build completes without TypeScript errors
  </acceptance_criteria>
  <done>GroupCreation multi-step flow works: contact picker with 12-member cap, details form with name/description, creates group via GroupAPI. GroupAvatarInitials renders CSS circle with deterministic color. GroupPrivacySetting provides 3-option radio for invite privacy. group-display.test.ts verifies injectGroupChat produces peerChat dialogs with negative peerId, and GroupAvatarInitials logic is correct.</done>
</task>

<task type="auto">
  <name>Task 2: GroupInfo sidebar + GroupMemberList + topbar wiring + startup loading</name>
  <files>src/components/nostra/GroupInfo.tsx, src/components/nostra/GroupInfo.module.scss, src/components/nostra/GroupMemberList.tsx, src/components/nostra/GroupMemberList.module.scss, src/lib/nostra/nostra-display-bridge.ts, src/lib/nostra/group-api.ts</files>
  <read_first>
    - src/components/peerProfile.tsx (sidebar info panel pattern to follow)
    - src/components/chat/topbar.ts (topbar tap handler to extend for groups)
    - src/components/popups/ (popup confirmation patterns)
    - src/lib/nostra/group-api.ts (addMember, removeMember, leaveGroup)
    - src/lib/nostra/group-store.ts (getGroupStore for metadata)
    - src/lib/nostra/virtual-peers-db.ts (getPubkey for member resolution)
    - .planning/phases/05-group-messaging/05-UI-SPEC.md (IC-05, IC-06 interaction contracts)
    - src/components/nostra/GroupAvatarInitials.tsx (created in Task 1)
  </read_first>
  <action>
    **Step 1 — GroupMemberList.tsx:** Per IC-05 member section:

    Props: `{groupId: string, members: string[], adminPubkey: string, isAdmin: boolean, onAddMember?: () => void}`

    - Render header "Members" with count `({members.length})`
    - If isAdmin: "Add Member" row at top with accent plus icon (triggers onAddMember callback)
    - Each member row: avatar (40px, from peer data or initials), name, role badge ("admin" pill — accent background, white text, 11px font per UI-SPEC)
    - Admin context action on non-admin members: right-click or long-press shows "Remove from Group" (danger color per UI-SPEC)
    - On remove: confirmation popup with copy from UI-SPEC copywriting contract:
      - Title: "Remove {Name} from {GroupName}?"
      - Primary button: "Remove {Name}" (danger background, white text)
      - Secondary button: "Keep Member" (secondary, default focus)
    - On confirm: call `getGroupAPI().removeMember(groupId, memberPubkey)`

    SCSS: `.memberRow` = 48px height, flex row with avatar + name + badge. `.adminBadge` = `background: var(--primary-color); color: white; font-size: 11px; padding: 2px 6px; border-radius: 8px`. Touch targets >= 44px.

    **Step 2 — GroupInfo.tsx:** Per IC-05 layout:

    Props: `{groupId: string}`

    Loads group from `getGroupStore().get(groupId)` on mount.

    Layout (top to bottom per IC-05):
    1. **Header:** GroupAvatarInitials (120px), group name (18px bold `var(--font-size-18)`), "{N} members" subtitle (singular: "1 member" per copywriting contract), description if set (14px, secondary color)
    2. **Actions row:** Mute notifications toggle (reuse tweb mute pattern), Search messages button
    3. **Members section:** `<GroupMemberList>` with isAdmin computed from `group.adminPubkey === ownPubkey`
    4. **Danger zone:** "Leave Group" button (danger color text, no background). If admin: "Delete Group" button below.
    - Leave confirmation popup per UI-SPEC:
      - Title: "Leave {GroupName}? You will need a new invite to rejoin."
      - Primary: "Leave Group" (danger background, white text)
      - Secondary: "Stay in Group" (secondary, default focus)
    - On leave confirm: `await getGroupAPI().leaveGroup(groupId)` then navigate away

    SCSS: Follow peerProfile.tsx sidebar layout. Header section with large avatar centered. Action row with horizontal button bar. Member section scrollable. Danger buttons at bottom.

    **Step 3 — Wire topbar for groups:**

    In the display bridge or chat topbar integration, detect when the active chat is a group peer (negative peerId in GROUP_PEER_BASE range). When user taps the topbar name/avatar area:
    - If group peer: mount GroupInfo.tsx in the right sidebar column (`right-column` in tweb)
    - Pass groupId (resolved from peerId via GroupStore.getByPeerId)
    - On handhelds (<= 600px): GroupInfo replaces chat view (full-screen panel per responsive spec)

    **Step 4 — Add member from GroupInfo:**

    When admin taps "Add Member" in GroupMemberList:
    - Open a contact picker (reuse GroupCreation's contact picker step, minus existing members)
    - On select: call `getGroupAPI().addMember(groupId, selectedPubkey)` for each selected contact
    - Show service message in chat for each added member

    **Step 5 — Initialize all group dialogs on app startup:**

    In nostra-display-bridge.ts initialization (or group-api.ts init), after loading identity:
    - Call `getGroupStore().getAll()` to load all groups
    - For each group: call `injectGroupChat(group)` to register in tweb's chat list
    - This ensures groups appear in chat list on app restart
  </action>
  <verify>
    <automated>pnpm test src/tests/nostra/group-display.test.ts && pnpm build 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/components/nostra/GroupInfo.tsx exists and contains `getGroupStore`
    - src/components/nostra/GroupInfo.tsx contains `Leave Group` (copy from UI-SPEC)
    - src/components/nostra/GroupInfo.tsx contains `120` (120px avatar size per UI-SPEC)
    - src/components/nostra/GroupInfo.tsx contains `members` (member count display)
    - src/components/nostra/GroupMemberList.tsx exists and contains `removeMember`
    - src/components/nostra/GroupMemberList.tsx contains `Add Member` (copy from UI-SPEC)
    - src/components/nostra/GroupMemberList.tsx contains `admin` (role badge)
    - src/components/nostra/GroupMemberList.tsx contains `Remove` (remove action)
    - src/components/nostra/GroupMemberList.module.scss contains `48px` or equivalent member row height
    - src/lib/nostra/nostra-display-bridge.ts contains `getAll()` (startup group loading)
    - pnpm build completes without TypeScript errors
    - pnpm test src/tests/nostra/group-display.test.ts passes (no regressions from Task 1 tests)
  </acceptance_criteria>
  <done>GroupInfo sidebar shows full group details with member list and admin actions. GroupMemberList supports add/remove with confirmation popups using exact UI-SPEC copy. Topbar tap opens GroupInfo for group chats. Groups load from IndexedDB on startup. group-display tests still green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of complete group messaging UI</name>
  <files>n/a</files>
  <action>Human verifies the group messaging UI end-to-end in browser.</action>
  <what-built>Complete group messaging UI: creation flow (contact picker -> details -> create), chat list entries with group avatars, group info sidebar with member management, service messages, and privacy settings</what-built>
  <how-to-verify>
    1. Open Nostra.chat in browser at localhost:8080
    2. Tap "New Group" button in sidebar
    3. Verify contact picker shows contacts with checkboxes and counter "N/12 members"
    4. Select 1-2 contacts, tap "Continue to Details"
    5. Enter a group name, tap "Create Group"
    6. Verify group appears in chat list with colored initials avatar
    7. Tap group in chat list, verify chat opens
    8. Tap group name in topbar, verify GroupInfo sidebar opens
    9. Verify member list shows members with "admin" badge on creator
    10. Verify "Leave Group" button is visible at bottom
    11. Check Settings > Privacy and Security for "Groups" privacy setting
  </how-to-verify>
  <verify>Visual inspection by user</verify>
  <done>User confirms group creation flow, chat list display, group info sidebar, member management, and privacy settings all work correctly.</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
```bash
pnpm test src/tests/nostra/group-display.test.ts
pnpm build
pnpm test
```
Display tests pass. Build completes without errors. All tests pass including new group tests from Plans 01 and 02.
</verification>

<success_criteria>
- GroupCreation flow: contact picker with 12-cap, details form, creates group via GroupAPI (per D-06, IC-01)
- GroupAvatarInitials: CSS circle with deterministic color from 7-color palette (per UI-SPEC)
- GroupInfo sidebar: avatar (120px), name, member count, member list, leave button (per IC-05)
- GroupMemberList: admin sees add/remove actions, confirmation popups with exact UI-SPEC copy (per IC-06, D-08)
- GroupPrivacySetting: 3-option radio (Everyone/Contacts/Nobody) per IC-07, D-07
- Groups load from IndexedDB on app startup and appear in chat list
- Topbar tap opens GroupInfo for group chats
- Service messages rendered for group events (per D-12, IC-04)
- All copy matches UI-SPEC copywriting contract exactly
- group-display.test.ts passes with tests for injectGroupChat and GroupAvatarInitials
- Build and full test suite pass
</success_criteria>

<output>
After completion, create `.planning/phases/05-group-messaging/05-03-SUMMARY.md`
</output>
</output>
