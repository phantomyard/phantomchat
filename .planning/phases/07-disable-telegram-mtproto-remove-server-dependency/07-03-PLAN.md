---
phase: 07-disable-telegram-mtproto-remove-server-dependency
plan: 03
type: execute
wave: 2
depends_on: [07-01, 07-02]
files_modified:
  - src/lib/apiManagerProxy.ts
  - src/index.ts
  - src/tests/nostra/boot-no-mtproto.test.ts
autonomous: true
requirements: [STUB-04, STUB-05]

must_haves:
  truths:
    - "Application starts and renders without any network requests to Telegram servers (*.telegram.org, DC IPs)"
    - "apiManagerProxy.loadAllStates() works with local IndexedDB only"
    - "apiManagerProxy.sendAllStates() does not trigger MTProto connections on worker side"
    - "Boot path in index.ts resolves without errors or MTProto connection attempts"
    - "All existing test suite passes (regression gate)"
  artifacts:
    - path: "src/lib/apiManagerProxy.ts"
      provides: "apiManagerProxy that works against local IndexedDB without MTProto"
    - path: "src/index.ts"
      provides: "Boot path that does not attempt Telegram connections"
    - path: "src/tests/nostra/boot-no-mtproto.test.ts"
      provides: "Test verifying no MTProto connections during boot"
      contains: "MTPROTO_DISABLED"
  key_links:
    - from: "src/index.ts"
      to: "src/lib/apiManagerProxy.ts"
      via: "loadAllStates/sendAllStates calls"
      pattern: "apiManagerProxy"
    - from: "src/lib/apiManagerProxy.ts"
      to: "src/lib/nostra/api-manager-stub.ts"
      via: "invokeApi rejection prevents MTProto from worker side"
      pattern: "MTPROTO_DISABLED"
---

<objective>
Ensure the boot path and apiManagerProxy work without triggering MTProto connections, and validate the entire phase with a boot-path test and regression suite.

Purpose: The app boot path (index.ts -> apiManagerProxy) must resolve cleanly with stubs in place. This plan verifies that loadAllStates/sendAllStates work against local IndexedDB and that no MTProto connections are attempted during startup. Also runs the full test suite as a regression gate.

Output: Updated apiManagerProxy.ts (if needed), updated index.ts (if needed), boot-no-mtproto.test.ts, full test suite green.
</objective>

<execution_context>
@/home/raider/.claude/get-shit-done/workflows/execute-plan.md
@/home/raider/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-disable-telegram-mtproto-remove-server-dependency/07-CONTEXT.md
@.planning/phases/07-disable-telegram-mtproto-remove-server-dependency/07-RESEARCH.md
@.planning/phases/07-disable-telegram-mtproto-remove-server-dependency/07-01-SUMMARY.md
@.planning/phases/07-disable-telegram-mtproto-remove-server-dependency/07-02-SUMMARY.md

<interfaces>
<!-- Key contracts from prior plans -->

From src/lib/nostra/api-manager-stub.ts (after Plan 01):
- All invokeApi() calls reject with {type: 'MTPROTO_DISABLED', code: 503} except P2P-bridged methods
- No fall-through to real MTProto

From src/lib/appManagers/networkerFactory.ts (after Plan 01):
- getNetworker() throws Error
- startAll(), stopAll(), forceReconnect(), forceReconnectTimeout() are no-ops

From src/lib/mtproto/authorizer.ts (after Plan 01):
- Defense-in-depth guard throws before handshake

From src/lib/mtproto/transports/controller.ts (after Plan 01):
- Defense-in-depth guard throws before transport creation

From src/components/connectionStatus.ts (after Plan 02):
- Listens to nostra_relay_state events
- No MTProto DC dependency
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Guard boot path, apiManagerProxy, and create boot-no-mtproto test</name>
  <files>src/lib/apiManagerProxy.ts, src/index.ts, src/tests/nostra/boot-no-mtproto.test.ts</files>
  <read_first>
    - src/lib/apiManagerProxy.ts
    - src/index.ts
    - src/lib/nostra/api-manager-stub.ts
    - src/lib/appManagers/networkerFactory.ts
  </read_first>
  <action>
**Analyze the boot path** by reading index.ts and apiManagerProxy.ts. The goal is to ensure no code path triggers MTProto connections during startup.

**apiManagerProxy.ts changes (if needed):**

Read the file to understand `loadAllStates()` and `sendAllStates()` flow. Per the research:
- `loadAllStates()` calls `loadStateForAllAccountsOnce()` which reads IndexedDB -- this should already work without MTProto
- `sendAllStates()` calls `this.invoke('state', ...)` to send state to SharedWorker -- the worker-side may trigger manager initialization that calls invokeApi()

If `sendAllStates()` triggers worker-side MTProto calls, those will now be rejected by the api-manager-stub from Plan 01. The key concern is whether those rejections cause unhandled promise rejections or retry loops.

Check if there are any direct MTProto method calls in apiManagerProxy.ts (like `invokeApi` calls for auth or updates). If found, guard them:

```typescript
// Guard any direct invokeApi calls that were for MTProto
// These are now no-ops since MTProto is disabled
```

**index.ts changes (if needed):**

Read index.ts looking for:
1. `randomlyChooseVersionFromSearch()` (Research Pitfall 6) -- this redirects to web.telegram.org. Remove the call or guard it with `if(false)` comment explaining it's disabled for Nostra.chat. Specifically: find the call site and wrap it:
   ```typescript
   // Nostra.chat: Telegram redirect disabled -- this is not a Telegram client
   // randomlyChooseVersionFromSearch();
   ```

2. Any direct MTProto imports or connection initialization beyond apiManagerProxy calls. If found, comment them out with `// Nostra.chat: MTProto disabled` marker.

3. Verify that `apiManagerProxy.sendEnvironment()`, `loadAllStates()`, `sendAllStates()` calls remain structurally unchanged (D-07).

**Error suppression for expected rejections:**

If the boot path produces console.warn messages from api-manager-stub (expected MTPROTO_DISABLED rejections from manager initialization), these are acceptable and should be left as warnings (not errors). Do NOT add blanket try/catch -- the warnings serve as documentation of what was being called.

However, if any code has `.then()` without `.catch()` that would cause an unhandled promise rejection, add a `.catch(noop)` to suppress it. Search for patterns like:
```typescript
this.invokeApi('...')  // without .catch()
```
in files that are called during boot (apiUpdatesManager.ts if it calls forceGetDifference during init).

**Create boot-no-mtproto.test.ts:**

Create `src/tests/nostra/boot-no-mtproto.test.ts` that verifies no MTProto connections during boot:

```typescript
describe('boot path without MTProto', () => {
  it('NetworkerFactory.getNetworker is stubbed and throws', () => {
    // Import NetworkerFactory, verify getNetworker throws 'MTProto disabled'
  });

  it('api-manager-stub rejects non-intercepted methods with MTPROTO_DISABLED', async () => {
    // Import and install api-manager-stub
    // Call invokeApi with a boot-time method like 'updates.getState'
    // Verify rejection with {type: 'MTPROTO_DISABLED'}
  });

  it('authorizer rejects with MTProto disabled', () => {
    // Import authorizer, verify main function throws/rejects
  });

  it('transport controller rejects with MTProto disabled', () => {
    // Import transport controller, verify creation throws/rejects
  });
});
```

This test file validates the boot-path safety net: even if the boot sequence tries to create networkers, authorize, or open transports, every layer rejects.
  </action>
  <verify>
    <automated>cd /home/raider/Repository/nostra && rtk pnpm test src/tests/nostra/boot-no-mtproto.test.ts && rtk pnpm test src/tests/nostra/mtproto-stub.test.ts && rtk pnpm test src/tests/nostra/connection-status-relay.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - index.ts does NOT contain an active (non-commented) call to `randomlyChooseVersionFromSearch()`
    - apiManagerProxy.ts `loadAllStates` method body does NOT contain `invokeApi` calls
    - If any boot-path invokeApi calls existed without .catch(), they now have .catch(noop) or equivalent
    - No new MTProto connection logic introduced
    - src/tests/nostra/boot-no-mtproto.test.ts exists and passes
    - boot-no-mtproto.test.ts verifies all defense-in-depth layers (NetworkerFactory, api-manager-stub, authorizer, transport controller)
  </acceptance_criteria>
  <done>Boot path resolves without MTProto connections. apiManagerProxy works against local storage. boot-no-mtproto.test.ts validates all stub layers. Expected rejection warnings appear in console but no unhandled promise rejections.</done>
</task>

<task type="auto">
  <name>Task 2: Full regression test suite</name>
  <files>src/tests/nostra/mtproto-stub.test.ts, src/tests/nostra/connection-status-relay.test.ts, src/tests/nostra/boot-no-mtproto.test.ts</files>
  <read_first>
    - src/tests/nostra/mtproto-stub.test.ts
    - src/tests/nostra/connection-status-relay.test.ts
    - src/tests/nostra/boot-no-mtproto.test.ts
  </read_first>
  <action>
Run the complete test suite to verify no regressions from the MTProto stubbing work:

1. Run `pnpm test` (full suite)
2. If any tests fail, analyze the failure:
   - If a test was relying on real MTProto `invokeApi()` fall-through, update it to expect MTPROTO_DISABLED rejection instead
   - If a test was importing `ConnectionStatus` from mtproto and testing DC-based flow, update it to test relay-based flow
   - If a test was calling `networkerFactory.getNetworker()`, update it to expect the throw
3. Fix any failures while preserving the test intent
4. Run full suite again to confirm green

Do NOT skip or delete existing tests. If a test needs to change, preserve its name and intent but update the assertion to match the new stubbed behavior.
  </action>
  <verify>
    <automated>cd /home/raider/Repository/nostra && rtk pnpm test</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test` exits with code 0 (all tests pass)
    - No test files deleted
    - src/tests/nostra/mtproto-stub.test.ts passes
    - src/tests/nostra/connection-status-relay.test.ts passes
    - src/tests/nostra/boot-no-mtproto.test.ts passes
  </acceptance_criteria>
  <done>Full test suite passes. All existing tests plus new MTProto stub, ConnectionStatus relay, and boot-no-mtproto tests are green. Phase 7 is complete.</done>
</task>

</tasks>

<verification>
- `pnpm test` exits 0 (full suite green)
- `grep -r 'randomlyChooseVersionFromSearch' src/index.ts` shows commented-out or removed call
- All 34+ existing test files still present in src/tests/nostra/
- New test files (mtproto-stub.test.ts, connection-status-relay.test.ts, boot-no-mtproto.test.ts) pass
</verification>

<success_criteria>
- Boot path works without MTProto connections
- apiManagerProxy loads/sends state via local IndexedDB only
- boot-no-mtproto.test.ts validates no MTProto connections at each defense layer
- Full test suite passes (regression gate)
- No unhandled promise rejections from expected MTPROTO_DISABLED rejections
</success_criteria>

<output>
After completion, create `.planning/phases/07-disable-telegram-mtproto-remove-server-dependency/07-03-SUMMARY.md`
</output>
